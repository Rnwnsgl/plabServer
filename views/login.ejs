<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>플랩 풋볼</title>
</head>
<style>
    .image-wrapper{
        width: 100%;
    }
    .image{
        max-width: 100%;
        height: auto;
    }
    .box{
        width: 300px;
        height: 30px; 
        border-radius: 5px;
        margin-bottom: 15px;
    }
</style>
<script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
<script>
    function loginUser() {
        var form = document.getElementById("login");
        axios({
            method: 'post',
            url: 'http://localhost:3000/login',
            data: {
                user_id: form.id.value,
                user_pw: form.pw.value
            }
        }).then((response) => {
            console.log("response.data:" , response.data);
            return response.data;
        }).then((data) => {
            if ( data.flag == false) {
                alert("계정 정보가 일치하지 않습니다. 다시 로그인해주세요");
            } else {
                alert("로그인 성공");
                var form_info = document.getElementById("form_info");
                form_info.id.value = form.id.value;
                form_info.submit();
            }     
        })
    }
</script>
<body>
    <div class="image-wrapper">
        <img class= "image" src= "https://yt3.googleusercontent.com/pWtHU31sgC0YZvHcHgL-40vUhwyJnGG0Z7CyKRzJRehsgmYSYeBPZipzJpdN0mMgpyWehXLB-w=s900-c-k-c0x00ffffff-no-rj" width="200px" >
    </div>
    <form id="login"> 
        <div>
            <label for="id"></label>
            <input name="id" type="email" class="box" style="width: 300px; height: 30px; margin-bottom: 20px;" placeholder="이메일을 입력해주세요" required />
        </div>
        <div>
            <label for="pw"></label>
            <input name="pw" type="password" class="box" placeholder="비밀번호를 입력해주세요" required /> 
        </div>
        <div id="button-group">
            <div>
                <button type="submit" class="box" style="background-color: rgb(47, 78, 255); color:white" onclick="loginUser()">로그인</button>
            </div>
            <div>
                <button type="button" class="box" style="background-color: yellow;">카카오 로그인</button>
            </div>
            <div>
                <button type="button" class="box" style="background-color: rgb(245, 121, 39);" onclick="location.href='signUp'">회원가입</button>
            </div>
            <div>
                <button type="button" class="box" onclick="location.href='admin'"">관리자모드</button>
            </div>
        </div>
    </form>  
    <form id="form_info" action="/edit" method="POST">
        <input type="hidden" name="id">
    </form>   
</body>
</html>